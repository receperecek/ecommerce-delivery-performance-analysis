# Data dictionary

The source tables are `orders`, `customers`, `order_items`, `order_payments`, `order_reviews`, `products`, `sellers`, and `product_category_name_translation`. See the source link in `data/README.md` for publisher documentation. The pipeline preserves source columns in `raw`, typed fields in staging, and controlled derived fields in analytics.
